# Copy to `.env` (same directory) and fill in. docker compose reads it automatically.
#
# Get a token from Buildkite: Agents -> your cluster -> Agent Tokens -> reveal an
# existing token or create a new one.
BUILDKITE_AGENT_TOKEN=bkat_replace_me
